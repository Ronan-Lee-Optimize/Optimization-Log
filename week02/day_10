# Day 10 - Review Day 03: for loop (Applied)
# opt-log | Consistency over perfection.

subjects = ["Math", "English", "Korean", "Science"]
hours = [3, 2, 4, 1]

for i in range(len(subjects)):
    print("Studied", subjects[i], "for", hours[i], "hour(s)")  # loop through two lists
